import { superstars } from "../js/data/superstars.js";import { decks } from "../js/data/decks.js";import { MatchEngine } from "../js/engine/MatchEngine.js";import { moveEligibility } from "../js/engine/rules.js";
const stars=Object.values(superstars);let issues=[];for(let i=0;i<stars.length;i++){const a=stars[i],b=stars[(i+1)%stars.length],g=new MatchEngine({p1:a,p2:b,decks,rng:()=>.42});const p=g.state().players.p1,lead=p.hand;if(lead.length!==5)issues.push(`${a.id}: Lead Off is ${lead.length}, expected 5`);const momentum=lead.filter(c=>c.kind==='momentum'),moves=lead.filter(c=>c.kind==='move');if(momentum.length!==2)issues.push(`${a.id}: Lead Off has ${momentum.length} Momentum, expected 2`);if(moves.length!==3)issues.push(`${a.id}: Lead Off has ${moves.length} Moves, expected 3`);const funded={agility:0,strength:0,strike:0,technical:0,...(a.entrance?.preMatchMomentum??{})};for(const c of momentum)funded[c.method]=(funded[c.method]??0)+(c.amount??1);const legalByMethods=moves.filter(c=>Object.entries(c.requirements??{}).every(([m,n])=>(funded[m]??0)>=n));if(!legalByMethods.length)issues.push(`${a.id}: no Lead Off move is method-funded after both opening Momentum pages + Entrance`);}console.log(JSON.stringify({superstars:stars.length,issues},null,2));if(issues.length)process.exit(1);
