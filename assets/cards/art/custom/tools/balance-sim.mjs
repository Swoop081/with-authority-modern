import { superstars } from "../js/data/superstars.js";import { decks } from "../js/data/decks.js";import { MatchEngine } from "../js/engine/MatchEngine.js";import { decisionOwner,cpuDecision,executeCpuDecision } from "../js/ai/WrestlingAI.js";
const stars=Object.values(superstars);function rng(seed){let x=seed>>>0;return()=>{x=(x*1664525+1013904223)>>>0;return x/4294967296;};}let matches=0,stalls=0,turns=0,finishes={};
for(let i=0;i<stars.length;i++)for(let j=0;j<stars.length;j++)if(i!==j){const g=new MatchEngine({p1:stars[i],p2:stars[j],decks,rng:rng(12345+i*101+j)});let steps=0;while(g.state().phase!=='MATCH_OVER'&&steps++<1500){const pid=decisionOwner(g.state()),d=cpuDecision(g,pid);if(!executeCpuDecision(g,d,pid))break;}matches++;turns+=g.state().turnNumber;if(g.state().phase!=='MATCH_OVER')stalls++;else finishes[g.state().finish?.type]=(finishes[g.state().finish?.type]||0)+1;}
console.log(JSON.stringify({matches,stalls,averageTurns:Number((turns/matches).toFixed(2)),finishes},null,2));if(stalls)process.exit(1);
