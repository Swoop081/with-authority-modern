import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";import { collectionCards } from "../js/data/collection.js";import { artworkFor } from "../js/data/artwork.js";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');let missing=[];for(const c of collectionCards){const u=artworkFor(c);if(!u){missing.push(c.id);continue;}const rel=u.split('?')[0].replace(/^\.\//,'');const f=path.join(root,rel);if(!fs.existsSync(f))missing.push(c.id);}console.log(JSON.stringify({cards:collectionCards.length,missing:missing.length,missingIds:missing.slice(0,20)},null,2));if(missing.length)process.exit(1);
