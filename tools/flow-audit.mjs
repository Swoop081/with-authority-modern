import { MatchEngine } from '../js/engine/MatchEngine.js'; import {superstars} from '../js/data/superstars.js';import{decks}from'../js/data/decks.js';import{cpuDecision,executeCpuDecision,decisionOwner}from'../js/ai/WrestlingAI.js';import{effectiveTotalMomentum}from'../js/engine/rules.js';import{isOffensiveMove}from'../js/data/move-types.js';
function seededRng(seed){let x=seed>>>0;return()=>((x=(1664525*x+1013904223)>>>0)/4294967296)}
for(const threshold of [0,2,3,4,5,6]){let terminal=0,move=0,pass=0,passWithMoves=0,ret=0;for(let rep=0;rep<3;rep++){const rs=Object.values(superstars); for(let i=0;i<rs.length;i++)for(let j=0;j<rs.length;j++)if(i!==j){const a=rs[i],b=rs[j],g=new MatchEngine({superstarA:a,superstarB:b,deckA:decks[a.id],deckB:decks[b.id],rng:seededRng(1000+rep*100+i*10+j)});let steps=0;while(g.state().phase!=='MATCH_OVER'&&steps<500){const o=decisionOwner(g.state());if(!o)break;const d=cpuDecision(g.state(),o);if(g.state().phase==='ACTION'&&['move','pass','returnToRing'].includes(d.type)&&effectiveTotalMomentum(g.state().players[o])>=threshold){terminal++; if(d.type==='move')move++;else if(d.type==='returnToRing')ret++; else {pass++;if(g.state().players[o].hand.some(c=>isOffensiveMove(c)))passWithMoves++;}}executeCpuDecision(g,o);steps++;}}}console.log(threshold,{terminal,move,pass,passWithMoves,ret,moveRate:move/terminal,deadPassRate:passWithMoves/terminal});}
