import { superstars } from "../js/data/superstars.js";import { createProfile,unlockSuperstar } from "../js/data/profile.js";import { grantBooster,openBooster } from "../js/data/boosters.js";import { sets } from "../js/data/sets.js";import { cardsForSet } from "../js/data/collection.js";
const stars=Object.values(superstars);const starter=stars.find(s=>['cm-punk','roman-reigns'].includes(s.id))?.id;if(!starter)throw new Error('No valid starter Superstar in build');const p=createProfile(starter);for(const s of stars)unlockSuperstar(p,s.id);let packs=0,cards=0,entrances=0,foilFirstFailures=0;
for(const setId of Object.keys(sets).filter(id=>cardsForSet(id).some(c=>c.kind!=='entrance'))){for(let i=0;i<20;i++){grantBooster(p,1,setId);const pack=openBooster(p,setId);if(!pack.length)break;packs++;cards+=pack.length;entrances+=pack.filter(c=>c.card?.kind==='entrance').length;if(!pack[0].foil)foilFirstFailures++;}}
console.log(JSON.stringify({packs,cards,entrancesInBoosters:entrances,foilFirstFailures},null,2));if(entrances||foilFirstFailures)process.exit(1);
