import { decks } from "./decks.js"; import { collectionCards } from "./collection.js"; const byId=new Map(collectionCards.map(c=>[c.id,c])); export function buildPlayableDeck(profile,sid){const saved=profile?.savedDecks?.[sid];if(Array.isArray(saved)&&saved.length===55){const x=saved.map(e=>byId.get(e.id??e)).filter(Boolean);if(x.length===55)return x;}return decks[sid]??[];} export function findPackUpgrades(){return [];} export function applyUpgrade(){return false;}
