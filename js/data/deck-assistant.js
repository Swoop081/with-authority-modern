import { decks } from "./decks.js?v=0.12.60"; import { collectionCards } from "./collection.js?v=0.12.60"; const byId=new Map(collectionCards.map(c=>[c.id,c])); export function buildPlayableDeck(profile,sid){const saved=profile?.savedDecks?.[sid];if(Array.isArray(saved)&&saved.length===60){const x=saved.map(e=>byId.get(e.id??e)).filter(Boolean);if(x.length===60)return x;}return decks[sid]??[];} export function findPackUpgrades(){return [];} export function applyUpgrade(){return false;}
